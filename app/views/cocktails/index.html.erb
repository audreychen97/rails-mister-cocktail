<h1>Cocktails</h1>
<% @cocktails.each do |cocktail| %>
  <div class="card"><%= link_to cocktail.name, cocktail_path(cocktail), class: "card-category"%></div>
<% end %>

<%= link_to "Create a New cocktail", new_cocktail_path, class: "btn btn-primary"%>
